# 任务：三个未完成的修复（重要：上次报告说做了但代码里没有）

技能：systematic-debugging、ponytail、ui-ux-pro-max-skill。
先输出方案报告，等确认后再动手。**必须真的改代码，改完后自己 grep 验证。**

═══════════════════════════
问题 1：回收箱返回键（代码里没做）
═══════════════════════════
现状：backButton 监听器里没有 bin-mask 的判断。
证据：grep "bin-mask" 只找到 CSS 定义（1492 行）、HTML（1887 行）、变量定义（5497 行），没找到 backButton 里的调用。

要求：
1. 找到 backButton 监听器（Capacitor App 插件）。
2. 在 App.exitApp() 之前加：
   if (binMaskEl.classList.contains('open')) {
     document.getElementById('bin-back').click();
     return;
   }
3. 改完后 grep "bin-back" 应该能看到 backButton 里的引用。

═══════════════════════════
问题 2：自动清理时机（代码里还是 20:00）
═══════════════════════════
现状：
- 1913 行文案："每天 20:00 自动执行。"
- 5622 行逻辑：if (now.getHours()*60 + now.getMinutes() < 20*60) return;
- 5616、6383 行注释也是"20:00"

要求：
1. 1913 行文案改成："每次打开 App 时检查并清理（每天一次）"
2. 删除 5622 行的 20:00 判断，改为"启动时无条件检查一次"
3. 更新 5616、6383 行的注释
4. 保留 auto_clean_last_date 机制（每天只执行一次）

改完后 grep "20:00" 应该只剩注释里的历史说明（或者完全没有）。

═══════════════════════════
问题 3：待办"进行中"标签（代码里没做）
═══════════════════════════
现状：grep "进行中" 只找到排序注释和手势注释，没有渲染标签的代码。

要求：
1. 在 renderList 里渲染待办卡片时，对"进行中"状态（type=todo 且 !doneAt 且 !isOverdue）加一个标签：
   <span class="inprog-flag">进行中</span>
2. CSS 加 .inprog-flag：
   - position: absolute; top: 12px; right: 12px;
   - font-size: 12px; color: #6B7280;
   - 与现有的 .due-badge 灰底圆角 chip 样式一致
   - 如果同时有倒计时角标，两个并排（倒计时在左，进行中在右）
3. 多选模式下隐藏（套用 #list-view:not(.multi) 或类似规则）

改完后 grep "inprog" 应该能看到渲染代码和 CSS。

═══════════════════════════
严格约束
═══════════════════════════
- 只改这三个问题。
- 不动图片、通知、更新弹窗。
- 纯原生 JS。
- **改完后必须自己 grep 验证**：
  · grep -c "bin-back" index.html（应 > 0）
  · grep -c "20:00" index.html（应为 0 或只在历史注释）
  · grep -c "inprog-flag" index.html（应 > 0）
- 报告时贴出这三条 grep 的实际输出。

═══════════════════════════
输出要求
═══════════════════════════
第一步（先输出）：
- 每个问题的根因确认（贴出当前代码行号）。
- 修改方案。

第二步（等确认后）：
- 实际修改。
- 报告文件大小、关键行号、三条 grep 的实际输出。
