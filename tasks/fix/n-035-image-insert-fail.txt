技能：先加载 systematic-debugging、ponytail、ui-ux-pro-max-skill。
先输出根因报告和方案，等确认后再动手。

═══════════════════════════
现象（关键证据）
═══════════════════════════
1. 浏览器里（http://10.80.184.150:8081/）：
   - 点"图片"按钮 → 能选图 → 选完什么反应都没有，没图片插入。
   - 没有弹出裁剪界面。
   - 页面本身可动（不是卡死）。
2. 真机 APK（1.6.3）里：
   - 点击编辑器内的图片 → 页面完全动不了（不是黑屏，就是卡在当前页）。
   - 系统侧边返回也退不出。
   - 大退重开还在卡死状态。

两条现象很可能是同一个根因：图片插入流程失败，导致编辑器里
存在"异常状态的 img"，点击它时触发 bug。

═══════════════════════════
根因排查
═══════════════════════════
请读并逐行分析以下函数：

1. handleImageFiles（L4259-4277）：
   a. currentId 在什么情况下会是 null？用户点图片时 currentId
      应该是什么值？
   b. readImageElement(file) 的 Promise 链，失败时 catch 里
      showUndoToast('图片加载失败') 会执行吗？
   c. cropQueue.push 之后 openCropUI(cropQueue[0]) 为什么可能
      不弹界面？

2. readImageElement 的完整实现（grep 找出来）：
   a. 用 FileReader 还是 createObjectURL？
   b. 失败路径是什么？
   c. 在浏览器和 Android WebView 里行为是否一致？

3. openCropUI（L4617-4658）：
   a. 第一行做什么？
   b. 有没有可能因为某个变量未定义或条件不满足直接 return？
   c. cropEl.classList.add('open') 时 CSS 是否真的能显示？
   d. 有没有把 img 放到 crop-img 的 src？

4. insertImageBlob（L4217-4258）：
   a. dbPutImage 是否可能失败但被静默吞掉？
   b. insertNodeAtCaret 在什么情况下会失败？
   c. img 标签的属性是否设置了 contenteditable、draggable、
      data-img-id？

5. 编辑器里插入的 img 标签，是否有可能出现"src 为空"、
   "objectURL 已 revoke"、"data-img-id 与 IndexedDB 不匹配"
   等异常状态？

6. 点击这样的"异常 img"时，会触发什么？
   - openViewer(src) 的 src 可能是什么值？
   - viewerImgEl.src = 一个无效 URL 时，会不会引起后续卡死？
   - 或者 contenteditable 会怎么处理这个 img？

必须明确回答：
- 浏览器里"选图后无反应"的精确原因（哪一行 return 了/哪一步
  抛异常被吞了）。
- APK 里"点击图片卡死"的精确原因（跟上面是不是同一根因）。
- 修复方案。

═══════════════════════════
修复方向（初步，以诊断为准）
═══════════════════════════
1. 修复 handleImageFiles / readImageElement / openCropUI 里
   的静默失败路径，加可见的错误提示。
2. 确保插入的 img 一定有有效 src 和 contenteditable="false"。
3. 打开便签时（hydrateImages）跳过无效 img，或清理掉。
4. 点击 img 前检查 src 和 data-img-id 是否有效，无效则忽略。
5. 添加全局 error 捕获，防止某个未处理异常导致后续事件全挂。

═══════════════════════════
严格约束
═══════════════════════
- 先诊断，不直接改。
- 只改图片插入/查看相关代码。
- 不动其他功能。
- 纯原生 JS。

═══════════════════════════
输出要求
═══════════════════════════
第一步（先输出）：
- 完整的根因分析（回答上述 6 点）。
- 修复方案（含具体代码行号）。

第二步（等确认后再做）：
- 实际修改。
- 报告文件大小、关键行号、验证方法。
