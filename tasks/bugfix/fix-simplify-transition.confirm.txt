═══════════════════════════════════════
方案确认与执行指令
═══════════════════════════════════════

报告质量很高。三个决策点全部确认，按以下执行。

═══════════════════════════════════════
问题 1：转场简化 — 同意"零改动"
═══════════════════════════════════════

你的发现很关键：源码里已经不存在 .pushed 动画了。
问题 1 不做任何改动。

但请补充一件事：
- 检查 index.html 和 www/index.html 是否真的同步。
- 检查 android/app/src/main/assets/public/index.html 也是最新版。
- 用 md5sum 确认三份一致。

用户会在重新打包后验证真机。如果真机还有"被推开"动画，
说明是安装包缓存问题——重装一次即可。

═══════════════════════════════════════
问题 2：编辑后返回卡顿 — 执行两个方案
═══════════════════════════════════════

【执行方案 1：删除调试 alert】
- 删除 L4819-4835 的 alert 探针（诊断窗口宽度用的）。
- 同时搜索文件里是否还有其他残留的 console.log / alert 调试代码，
  如果有，一起删。
- 这个必须删，它每次启动 1.5 秒后弹窗，本身就破坏体验。

【执行方案 2：摘要测量推迟到滑出后】
- 按你分析：backToList() L3538-3550 的摘要测量在返回首帧
  执行，落在滑出动画第 1 帧，导致列表中上部闪一下。
- 修改：把测量逻辑的执行条件从"列表是否 active"改为"编辑页是否还在"。
  即：等编辑页完全滑出隐藏后再测量。
- 具体：把 L3541 附近的 rAF 条件改为
  if (!editorView.classList.contains('active')) { ... 测量逻辑 ... }
  让测量在编辑页滑出完成后才执行。
- 只改这一处条件，不动测量逻辑本身。

【C-c 不动】
- .editor-panel 的 backdrop-filter 参与滑出合成，暂时不动。
- 等真机验证方案 1+2 是否有效，如果还有卡顿，再回头处理 C-c。

═══════════════════════════════════════
执行顺序
═══════════════════════════════════════

1. 检查三份 HTML 是否 md5 一致。
2. 删除调试 alert。
3. 修改摘要测量的时机。
4. md5sum 重新确认一致。
5. 报告：
   - 修改的行号
   - 删除了哪些调试代码
   - md5sum 结果

═══════════════════════════════════════
严格约束
═══════════════════════════════════════

- 问题 1 零改动。
- 问题 2 只改调试 alert 删除 + 摘要测量时机。
- 不动布局宽度。
- 不改功能逻辑。
- 保留液态玻璃视觉效果。
- 保留 .no-blur 冻结逻辑。
- 保留上几轮修复的成果。
- 不改转场动画本身（编辑页滑入/滑出）。
